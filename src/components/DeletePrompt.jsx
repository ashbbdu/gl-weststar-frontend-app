import { Button } from '@mui/material';
import React from 'react';

const DeletePrompt = ({ setOpen}) => {
    
// const [open ] = useState(false)
  return (
    <div>
        <Button>Confirm</Button>
        {/* <Button onClick={setOpen(false)}>Cancel</Button> */}
    </div>
  )
}

export default DeletePrompt